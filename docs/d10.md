# DELI 10 - Process Tree Relationships

## Objective
- implement process hierarchy and relationship management
    
## Learnings
- lotsa practice of walking thru singly linked lists 
- child insertion does not care about preserving order, removal needs to locate the siblings by walking the parent-child linked list 
- detached processes to be isolated, parent and sibling NUlled
- defensive checks for NULL process at all stages
- attach operation: O(1), head insertion
- detach operation: O(siblings) 
- unit tests:
    - attach
    - head detach
    - middle detach
    - tail detach
    - empty tree

## Outcomes
implemented: `atta_chil()` attach, `deta_chil()` detach, `test_tree()` and `proc_tree_prin()`
- memory validation valgrind
    - 5 allocs
    - 5 frees
    - 0 leaks
    - 0 erros

now both lookup and relationships are handled i can start working on proc conn yippee
