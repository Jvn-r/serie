# DELI 2 - Reading PID and PNAMES and geting BIRTH and DEATH of processes

## Objective
read the proc info from a py script

## Learnings
- with os library, doing listdir, filtering out non numeric ones coz they're not PID's
- with open /proc/{pid}/comm to get name
- and storing all of em in a dict {PID:PNAME}
- looping with sleep 1 second, to get proc names and ids every second 

## Outcome
a dict with pid and pname, each second
